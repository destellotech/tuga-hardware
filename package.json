{
  "name": "tuga-hardware",
  "private": true,
  "type": "module",
  "description": "tugahardware.com — static site generator + Cloudflare Worker. All HTML is generated: edit build/, data/products.json or build/content/, then run `npm run build`.",
  "scripts": {
    "build": "node build/minify.mjs && node build/build.mjs",
    "preview": "python -m http.server 4321",
    "deploy": "npm run build && wrangler deploy",
    "images": "node build/optimise-images.mjs",
    "test": "node --test \"test/*.test.mjs\""
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "sharp": "^0.35.3"
  }
}
